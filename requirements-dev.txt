# Test and lint tooling only, pinned for the same reason the runtime tree is:
# a ruff release should change the build when we choose it to, not when it
# happens to be published. Transitives are listed too.
#
# Deliberately does NOT `-r requirements.lock`. Install both together:
#
#     pip install -r requirements.lock -r requirements-dev.txt
#
# Keeping them separate is what lets Dependabot manage this file and
# requirements.txt while leaving the generated lock alone -- see the note in
# .github/dependabot.yml for why that separation matters.
aiosqlite==0.22.1
coverage==7.16.0
httpcore==1.0.9
httpx==0.28.1
iniconfig==2.3.0
packaging==26.3
pluggy==1.6.0
Pygments==2.21.0
pytest==9.1.1
pytest-asyncio==1.4.0
ruff==0.16.5
