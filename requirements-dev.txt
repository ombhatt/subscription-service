# Everything the service runs with, at the exact versions it will run with.
# CI installs this, so the suite tests the same tree the image ships.
-r requirements.lock

# Plus the tools that only exist to test and lint it, pinned for the same
# reason: a ruff release should change the build when we choose it to, not
# when it happens to be published. Transitives are listed too.
aiosqlite==0.22.1
httpcore==1.0.9
httpx==0.28.1
iniconfig==2.3.0
packaging==26.3
pluggy==1.6.0
Pygments==2.21.0
pytest==9.1.1
pytest-asyncio==1.4.0
ruff==0.16.5
