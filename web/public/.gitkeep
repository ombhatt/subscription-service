Next.js serves this directory at the site root. It is empty today, but the
production image copies it unconditionally (the standalone build output does
not include it), so it has to exist or the image build fails on a COPY.
