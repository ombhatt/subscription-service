# The libraries this service depends on directly, with the floor for each.
# This file records *intent*; requirements.lock records what actually gets
# installed, transitive dependencies and all, and is what every build reads.
# Change a floor here, then `make lock` and run the suite.
fastapi>=0.115
uvicorn[standard]>=0.32
pydantic>=2.9
pydantic-settings>=2.6
sqlalchemy[asyncio]>=2.0.36
asyncpg>=0.30
alembic>=1.14
redis>=5.2
stripe>=11.1
python-dotenv>=1.0
pyjwt[crypto]>=2.10
prometheus-client>=0.21
